rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    // Bill-scan upload paths are added in the purchases/sales milestone.
    // Deny everything by default until then.
    match /{allPaths=**} {
      allow read, write: if false;
    }
  }
}
